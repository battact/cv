{
  "name": "cv-project",
  "private": true,
  "version": "1.0.0",
  "description": "CV Website with Infrastructure",
  "scripts": {
    "dev": "cd ui && npm run dev",
    "build": "cd ui && npm run build",
    "lint": "cd ui && npm run lint",
    "preview": "cd ui && npm run preview",
    "deploy": "cd ui && npm run deploy",
    "deploy:infra": "cd infra && npm run cdk deploy",
    "deploy:website": "cd ui && npm run deploy:website",
    "install:all": "npm install && cd ui && npm install && cd ../infra && npm install",
    "clean": "cd ui && rm -rf dist node_modules .vite && cd ../infra && rm -rf dist node_modules"
  },
  "workspaces": [
    "ui",
    "infra"
  ],
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
} 